name: PyPI-ga avtomatik yuklash

on:
  push:
    tags:
      - 'v*' # Faqat v1.0.0, v1.0.1 kabi versiya teglari push qilinganda ishlaydi

jobs:
  build-n-publish:
    name: Paketni yig'ish va PyPI-ga jo'natish
    runs-on: ubuntu-latest

    steps:
    - name: Kodni yuklab olish
      uses: actions/checkout@v3

    - name: Python muhitini sozlash
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: Build vositalarini o'rnatish
      run: |
        python -m pip install --upgrade pip
        pip install build

    - name: Paketni arxivlash (Build)
      run: python -m build

    - name: PyPI-ga xavfsiz yuklash
      uses: pypa/github-action-pypi-publish@release/v1
      with:
        password: ${{ secrets.PYPI_API_TOKEN }}
